# VM 耦合边界类清单：公开包（java/）里由 JVM 自身引导、承载 VM 设施的类。
#
# 这些类在 HotSpot 中由 VM 本地代码直接创建 / 驱动（initPhase1~3、模块系统引导、
# 类加载、安全管理器），在原生二进制里没有字节码层面的对应物。它们与内部包
# （jdk/internal/、sun/）同规则处理：调用链 BFS 在此截断，类整体视为手写边界类，
# 被调用到的方法在 runtime/ 对应目录按需手写，未实现的方法保持 panic!("stub: ...")。
#
# 引入动机：类初始化（JVMS §5.5）进入调用链后，<clinit> 的静态可达闭包会沿
# 安全管理器 → 模块层引导 → 虚拟线程调度器 一路展开到整个并发 / 模块 / 反射子系统，
# 而这些路径在运行时永不执行（JDK 21 安全管理器恒为 null）。
#
# 格式：每行一个类的 binary name（其嵌套类一并视为边界），`#` 开头为注释。

java/lang/SecurityManager
java/lang/VirtualThread
java/lang/ClassLoader
java/lang/Module
java/lang/ModuleLayer
java/lang/Class

# POSIX 原生族档 A（2026-09-23）：FileSystems.getDefault 的默认持有者链
# （DefaultFileSystemHolder → $1 → getDefaultProvider → getFileSystem("file:///")）
# 中，getFileSystem 为 UnixFileSystemProvider 的 final 声明——生成侧继承成员的
# 需求登记不覆盖「静态类型接收者 + 抽象槽位」形态，平台 provider 子类（sun/ 存根）
# 的 vtable 槽位缺失。类按边界惯例整体手写：getDefault 以
# DefaultFileSystemProvider.theFileSystem() 等价承载（doPrivileged 在无安全管理器
# 时透明，JDK 可观测行为一致）。其余成员保持 panic 存根。
java/nio/file/FileSystems
